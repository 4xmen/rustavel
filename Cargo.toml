[workspace]
members = [
    "app",
    "core",
    "artisan",
    "database",
    "macros",
    "macros-core",
    "integration-tests",
    #    "graveyard", # uncomment this workspace carfully this not for production
]
resolver = "2"

[workspace.dependencies]

# ── Runtime / Async ───────────────────────────────
tokio = { version = "1.52", features = ["full"] }
futures = "0.3"

# ── Web Framework / HTTP / FORM ──────────────────────────
axum = { version = "0.8", features = ["ws", "macros"] }
axum-extra = { version = "0.12.6", features = ["routing", "typed-routing"] }
tower = "0.5.3" # middleware / layers
axum-params = "0.4"

# ── Templates / Views + Code genrate ─────────────────────────────
minijinja = { version = "2", features = ["loader"] } # Jinja-like templates

# ── Database / Persistence / fake ────────────────────────
sqlx = { version = "0.8.6", features = ["runtime-tokio", "tls-rustls-ring-webpki", "mysql", "sqlite", "postgres", "macros",  "time"] }
fake = "5"
# ── Serialization / Data ──────────────────────────
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# ── CLI / Config ──────────────────────────────────
clap = { version = "4.5.56", features = ["derive"] }
dotenv = "0.15.0" # env loading (dev)

# ── Utilities / Glue ──────────────────────────────
once_cell = "1.21.3"     # lazy statics
async-trait = "0.1.89"   # async in traits

# ── Time / Date ───────────────────────────────────
time = { version = "0.3", features = ["macros", "parsing", "formatting", "local-offset", "serde"] } # modern time handling

# ── String and utilty  ─────────────────────────
regex = "1.12.2" # str
illuminate-str = "0.3" #str
rand = "0.10" #random

# ── Terminal / UX ─────────────────────────
colored = "3"
terminal_size = "0.4"
ansi-str = "0.9"
dialoguer = "0.12"

# ── Crypto / Security ─────────────────────
ring = "0.17"            # crypto + secure hash
crc32fast = "1.5"        # cheap integrity hash

# ── Encoding / Web ────────────────────────
data-encoding = "2.10"    # binary encodings
percent-encoding = "2.3" # URL encode/decode

